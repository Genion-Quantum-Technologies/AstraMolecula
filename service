#!/bin/bash

# DockingVina服务管理快捷方式  
# 重定向到cicd/scripts/start_docking_service.sh

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/cicd/scripts/start_docking_service.sh" "$@"
